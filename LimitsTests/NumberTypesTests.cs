using Microsoft.VisualStudio.TestTools.UnitTesting;

using System;
using System.Linq;
using System.Collections.Generic;

namespace LimitsTests.NumberTypes
{
    using Limits.NumberTypes;

    [TestClass]
    public partial class LimitsTests
    {
        [TestMethod]
        public void Test1()
        {
            //var t = new Tuple<Two, int>(new PairElement<int>[] {
            //    new PairElement<int>(1),
            //    new PairElement<int>(2)
            //});
            //var t = new Tuple<Three, int>(new PairElement<int>[] {
            //    new PairElement<int>(1),
            //    new PairElement<int>(2),
            //    new PairElement<int>(3)
            //});
            //Assert.IsInstanceOfType(t.a, typeof(PairElement<int>));
            //Assert.AreEqual((t.a as PairElement<int>).x, 1);
            //Assert.IsInstanceOfType(t.b, typeof(OrderedPair<int>));
            //Assert.IsInstanceOfType((t.b as OrderedPair<int>).a, typeof(PairElement<int>));
            //Assert.AreEqual(((t.b as OrderedPair<int>).a as PairElement<int>).x, 2);
            //Assert.AreEqual(((t.b as OrderedPair<int>).b as PairElement<int>).x, 3);
        }
    }
}
